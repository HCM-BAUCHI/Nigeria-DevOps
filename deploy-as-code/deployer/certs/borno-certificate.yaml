apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: borno-hcm.digit.org-tls-certs
  namespace: egov
spec:
  secretName: borno-hcm.digit.org-tls-certs
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: borno-hcm.digit.org
  dnsNames:
    - borno-hcm.digit.org


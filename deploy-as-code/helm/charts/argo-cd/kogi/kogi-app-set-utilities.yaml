apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kogi-appset-utilities
  namespace: argocd
spec:
  generators:
    - list:
        elements: []

  template:
    metadata:
      name: 'kogi-{{name}}'

    spec:
      project: kogi-project

      source:
        repoURL: git@github.com:HCM-BAUCHI/Nigeria-Devops.git
        targetRevision: ng-central-prd
        path: deploy-as-code/helm/charts/health-services/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../environments/kogi-central-prd.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: kogi

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 10
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s

      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas